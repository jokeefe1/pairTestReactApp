import React from 'react';
import { BrowserRouter, Switch } from 'react-router-dom';

const AppRouter = () => (
    <BrowserRouter>
        <Switch />
    </BrowserRouter>
);

export default AppRouter;
